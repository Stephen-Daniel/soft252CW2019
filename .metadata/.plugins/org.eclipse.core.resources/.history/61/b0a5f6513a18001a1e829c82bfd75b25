package SecretarySystem;

import java.io.File;
import java.io.FilenameFilter;

import LoginSystem.Person;

public class secretary {
		
	public File[] tempFiles;
	public int i = 0;
		public secretary()
		{
			
		}
		
		public void renameFile(String filename, String uniqueNumber)
		{
			uniqueNumber = uniqueNumber + ".txt";
			
			File approved = new File(System.getProperty(filename));
			File rename = new File(System.getProperty(uniqueNumber));
						
			if (approved.renameTo(rename)) {
				//test
				System.out.println(filename + " " + uniqueNumber + " " + rename.getName());
				}else {
					System.out.println("error");
				}
			}
		
		public void notApproved(String filename)
		{
			System.out.println(filename);
			
			File notApproved = new File(filename);
			
			notApproved.delete();
			
		}

		public File[] FindTextFiles(String[] temps)
		{						
				File directoryPath = new File(System.getProperty("user.dir"));			
				
				File[] files=directoryPath.listFiles(new FilenameFilter() {
					@Override
					public boolean accept(File dir, String name) {
						
						return name.startsWith("temp");
					}
				});
											
				return tempFiles;
			}
		}

